import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    textBanner: {
        fontSize: 40,
        color: "white",
        marginTop: 15,
        marginLeft: -50,
        fontWeight: "Bold"
      },
    imgBanner: {
      width: "100vw",   
      height: 300,
      marginTop: 20,
      borderRadius: 10
      }
      
})

export default styles;