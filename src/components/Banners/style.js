import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    textBanner: {
        fontSize: 300,
        color: "white",
        marginTop: 15,
        marginLeft: -180,
        fontWeight: "Bold"
      },
    imageBanner: {
    width: "90%",   
    height: 200,
    marginTop: 15,
    borderRadius: 10
  }
})

export default styles;